<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>REDfiniti Container Tracking</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Tailwind CSS via CDN -->
  <script src="https://cdn.tailwindcss.com"></script>

  <style>
    /* Optional: smooth fade for result card */
    .fade-in {
      animation: fadeIn 0.4s ease-out;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(8px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body class="min-h-screen bg-slate-950 text-white">
  <!-- Background (blurred gradient / subtle pattern) -->
  <div
    class="fixed inset-0 -z-10 bg-gradient-to-br from-slate-900 via-slate-950 to-slate-900"
  >
    <div
      class="absolute inset-0 bg-[radial-gradient(circle_at_top,_rgba(239,68,68,0.35),_transparent_60%),_radial-gradient(circle_at_bottom,_rgba(59,130,246,0.35),_transparent_60%)] blur-3xl opacity-60"
    ></div>
  </div>

  <div class="flex min-h-screen items-center justify-center px-4 py-10">
    <!-- Glassmorphism Card -->
    <div
      class="w-full max-w-3xl rounded-3xl bg-white/10 border border-white/15 backdrop-blur-2xl shadow-[0_20px_60px_rgba(0,0,0,0.55)] p-8 md:p-10 space-y-8"
    >
      <!-- Header -->
      <header class="flex flex-col md:flex-row md:items-center md:justify-between gap-6">
        <!-- Logo placeholder -->
        <div
          class="flex items-center justify-center h-16 w-32 rounded-2xl border border-white/20 bg-white/5 text-sm uppercase tracking-[0.2em] text-white/60"
        >
          Freight Logo
        </div>

        <!-- Title / Subtitle -->
        <div class="text-right md:text-right flex-1">
          <h1 class="text-2xl md:text-3xl font-semibold tracking-wide">
            RED<span class="text-red-400">finiti</span> Container Tracking
          </h1>
          <p class="mt-1 text-sm text-white/60">
            Real-time visibility for your ocean shipments.
          </p>
        </div>
      </header>

      <!-- Today's Tracking Section -->
      <section class="space-y-6">
        <h2 class="text-lg font-semibold tracking-wide text-white">
          Today's Tracking
        </h2>

        <!-- Form -->
        <form id="trackingForm" class="grid gap-4 md:grid-cols-[1.2fr,1.5fr,0.8fr] items-end">
          <!-- Shipping Line Dropdown -->
          <div class="space-y-2">
            <label for="shippingLine" class="text-xs font-medium uppercase tracking-wide text-white/60">
              Shipping Line
            </label>
            <select
              id="shippingLine"
              class="w-full rounded-xl bg-white/5 border border-white/20 px-3 py-2.5 text-sm outline-none focus:border-red-400 focus:ring-1 focus:ring-red-400/80 text-white/90"
            >
              <option value="" disabled selected>Select a shipping line</option>
              <option value="MACS">MACS</option>
              <option value="MSC">MSC</option>
              <option value="Bobs shipping">Bobs shipping</option>
            </select>
          </div>

          <!-- Container Number Input -->
          <div class="space-y-2">
            <label for="containerNumber" class="text-xs font-medium uppercase tracking-wide text-white/60">
              Container Number
            </label>
            <input
              id="containerNumber"
              type="text"
              placeholder="e.g. FCIU5935233"
              class="w-full rounded-xl bg-white/5 border border-white/20 px-3 py-2.5 text-sm outline-none focus:border-red-400 focus:ring-1 focus:ring-red-400/80 text-white/90 placeholder:text-white/30"
            />
          </div>

          <!-- Go Fetch Button -->
          <div class="space-y-2">
            <span class="block text-xs font-medium uppercase tracking-wide text-transparent select-none">
              &nbsp;
            </span>
            <button
              type="submit"
              class="w-full rounded-xl bg-red-500/90 hover:bg-red-400 active:bg-red-500 text-sm font-semibold tracking-wide py-2.5 shadow-lg shadow-red-500/30 transition"
            >
              Go Fetch
            </button>
          </div>
        </form>
      </section>

      <!-- Divider -->
      <div class="border-t border-white/10"></div>

      <!-- Result Section -->
      <section class="space-y-4">
        <h3 class="text-sm font-semibold tracking-wide uppercase text-white/60">
          Tracking Result
        </h3>

        <div
          id="resultCard"
          class="hidden fade-in rounded-2xl bg-black/30 border border-white/15 px-5 py-4 md:px-6 md:py-5 text-sm md:text-base"
        >
          <dl class="grid gap-3 md:grid-cols-2">
            <div>
              <dt class="text-xs uppercase tracking-wide text-white/50">
                Container Number
              </dt>
              <dd class="mt-1 font-medium text-white">
                FCIU5935233
              </dd>
            </div>

            <div>
              <dt class="text-xs uppercase tracking-wide text-white/50">
                Shipped From
              </dt>
              <dd class="mt-1 text-white">
                Koblenz am rhein, DE
              </dd>
            </div>

            <div>
              <dt class="text-xs uppercase tracking-wide text-white/50">
                Port of Load
              </dt>
              <dd class="mt-1 text-white">
                Rotterdam, NL
              </dd>
            </div>

            <div>
              <dt class="text-xs uppercase tracking-wide text-white/50">
                Port of Discharge
              </dt>
              <dd class="mt-1 text-white">
                Durban, ZA
              </dd>
            </div>

            <div>
              <dt class="text-xs uppercase tracking-wide text-white/50">
                Shipped To
              </dt>
              <dd class="mt-1 text-white">
                Durban, ZA
              </dd>
            </div>

            <div>
              <dt class="text-xs uppercase tracking-wide text-white/50">
                Transhipment
              </dt>
              <dd class="mt-1 text-white">
                Yes (via mainline hub)
              </dd>
            </div>

            <div class="md:col-span-2">
              <dt class="text-xs uppercase tracking-wide text-white/50">
                Price Calculation Date*
              </dt>
              <dd class="mt-1 text-white">
                31/12/2025
              </dd>
            </div>
          </dl>

          <p class="mt-4 text-[11px] text-white/50">
            * All information above is dummy data for demonstration purposes only.
          </p>
        </div>
      </section>
    </div>
  </div>

  <!-- Simple JS to show dummy result on submit -->
  <script>
    const form = document.getElementById("trackingForm");
    const resultCard = document.getElementById("resultCard");

    form.addEventListener("submit", (e) => {
      e.preventDefault();

      // In a real app you would use the form data here.
      // For now we just show the static dummy result.
      resultCard.classList.remove("hidden");
      resultCard.classList.add("fade-in");
    });
  </script>
</body>
</html>